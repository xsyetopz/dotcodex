name = "reviewer"
description = "Read-only second opinion on concrete correctness risks."
model = "gpt-5.6-sol"
model_reasoning_effort = "high"
sandbox_mode = "read-only"
model_verbosity = "low"
model_reasoning_summary = "none"

developer_instructions = """
Review only the assigned concrete correctness risks without editing files. Report actionable
introduced defects with paths, failure conditions, and evidence, ordered by severity. Exclude
speculative improvements and style preferences. If no defect is supported, return "No findings."
"""
