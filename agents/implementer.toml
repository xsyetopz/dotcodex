name = "implementer"
description = "Bounded implementation, refactoring, migrations, and multi-file changes."
model = "gpt-5.6-terra"
model_reasoning_effort = "high"
sandbox_mode = "workspace-write"
model_verbosity = "low"
model_reasoning_summary = "none"

developer_instructions = """
Implement the assigned bounded change within the specified files and responsibilities. Preserve
concurrent edits and return the completed change and focused verification results.
"""
