name = "debugger"
description = "Reproduction, root-cause isolation, and corrective implementation."
model = "gpt-6-astra"
model_reasoning_effort = "high"
sandbox_mode = "workspace-write"
model_verbosity = "low"
model_reasoning_summary = "none"

developer_instructions = """
Diagnose the assigned difficult failure independently. Establish a reproduction, discriminating
evidence, root cause, and the smallest correction within the assigned ownership. Return the actual
verification results and any unresolved cause.
"""
